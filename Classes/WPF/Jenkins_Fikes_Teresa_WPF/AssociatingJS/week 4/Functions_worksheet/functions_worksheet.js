/**
 * Teresa Jenkins Fikes
 *  WPF 1408 Section 01
 * Screencast Functions week 4
 * 10/21/14.
 */


//Calculate the Circumference of a circle

var diameter = 2;

var Circumference = calcCircumference(2 * (Math.PI * rad))

//This is the function that performs the calculation:

//it takes the radius as input and returns the circumference

function calcCircumference(rad)

{

    var circumference = 2 * (Math.PI * rad);

    return circumference;

}
console.log("The circumference of the circle is X")


